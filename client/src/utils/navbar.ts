import { useRoute } from "vue-router"

const router = useRoute()

export const mainMenu = []

export const userMenu = [
  {
    name: "Mi Perfil",
    handler() {
      // router.name("")
    }
  }
]
